# NanHaiVoyage 0.28.10 — minimap art, world×2, redeem codes, HUD polish

## Model
`gpt-6-astra` **high**, `/fast` **off**, never `gpt-5.6-luna`. Visible Codex terminal.
First reply: quota OK or exact usage-limit/reset.

## Refs (user screenshots)
- `assets/ui-refs/15-minimap-port-white-bg.jpg` — white boxes behind port/island stickers on minimap/chart
- `assets/ui-refs/16-voyage-hud-quest-footer.jpg` — crowded harbors + bottom blessing + quest card chrome

## 1) Minimap / chart port-island stickers: remove white background
Port/island thumbnails on the **round minimap** and **full 南海海图** show opaque white rectangles behind the art. Make them **transparent** (premultiply / strip near-white matte / draw with blending, or process PNGs once). Art silhouette only — no white card. Keep hit targets usable.

## 2) Double world / harbor spacing
Harbors feel too close in chase view (see ref 16). **Scale the playable world ~2×**:
- `Catalog.WORLD_W` / `WORLD_H` (currently 4800×3600) → ~9600×7200
- Scale all `PORT_X/Y`, `ISLAND_X/Y` (and any land rects / spawn / pirate ranges tied to absolute coords) consistently so relative layout is preserved but distances double
- Update collision, dock, auto-sail, minimap projection, camera far plane / water tiling as needed so nothing breaks
- User will retune later; this pass is “about 2× larger,” not perfect final balance

## 3) 福利 → fullscreen redeem-code page
Replace toast `福利尚未开放` with a **fullscreen** overlay (same family as other fullscreen panels; port/island interaction style unchanged).
- Input field for redeem code + confirm
- Codes (case-sensitive or trim OK; document):
  - `666` → +666 银两
  - `888` → +888 银两
- Persist silver; prevent trivial infinite spam if easy (e.g. each code redeemable once per save) — simple once-per-code is fine for now
- Close returns to voyage HUD; world pauses while open

## 4) Remove bottom blessing line
Remove the bottom-center blessing / tip text such as `祝您一路顺风…` / seagull weather blessing bar content the user called out. Do not leave an empty ugly bar if it only existed for that copy — hide or delete the widget.

## 5) Right quest card: title vs corner triangle overlap
Active-quest card on the right: first character of the title overlaps the decorative gold triangle at the bottom-left of the header. Fix padding/layout so title clears the icon (see ref 16).

## Package
When all five done:
```bash
./scripts/release.sh 0.28.10 "小地图去白底；世界约2倍；福利兑换码；去祝福语；任务卡标题避让"
```
APK stays gitignored under `Builds/`. Report Release URL.
