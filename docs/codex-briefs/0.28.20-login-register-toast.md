# NanHaiVoyage 0.28.20 — Login/Register success popup (FreeBuff / GLM)

## Agent
**FreeBuff** visible terminal at `~/shipgame`. **Model: GLM 5.3 Flash**.
Base: `main` after **v0.28.19** is released (must be newer than 0.28.19 so update check works).

## Package
```bash
./scripts/release.sh 0.28.20 "登录成功与注册成功弹窗"
```
APK gitignored. Report GitHub Release URL.

---

## Goal
After successful **login** and successful **register** on LoginScreen, show a short confirmation modal/dialog (same style family as existing login modals / update dialog — Tang UI), e.g.:
- 登录成功 → confirm → proceed into game (existing navigation)
- 注册成功 → confirm → then continue (either auto-login into game or stay ready to login — prefer proceed into game if current register already logs in)

Keep copy short. One primary button「好的」/「进入」.

## Out of scope
Version-gate redesign (already in 0.28.19), audio, sea maps.

## Success
Release **v0.28.20** with success popups; tag newer than v0.28.19.
