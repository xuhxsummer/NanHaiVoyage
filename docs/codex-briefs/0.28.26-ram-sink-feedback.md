# NanHaiVoyage 0.28.26 — Medium ram / sink feedback (Codex or FreeBuff)

## Agent
Prefer **Codex** visible terminal `~/shipgame` (gpt-6-astra, high, /fast off) for VFX/feel; FreeBuff GLM OK if Codex quota empty.
Base: `main` after **v0.28.25** lands.
Do **not** start until 0.28.25 Release exists.

## Package
```bash
./scripts/release.sh 0.28.26 "撞船晃闪水花；沉船倾侧气泡跟镜；战利品稍后弹出"
```

## Locked with Summer (medium tier)
No wood-break SFX. Keep Water Splash 02 + BGM duck for sink.

### Ram / collision
1. Short hull rock/tilt on both ships (~0.2–0.4s) on top of existing mutual push.
2. Small splash burst at contact point (brief, local — not fullscreen).
3. Hurt flash: damaged ship (esp. player) brief red flash / HP bar pulse.
4. Existing impact SFX OK; slightly louder if too quiet; **no** wood crack.
5. Optional tiny camera punch on hit (subtle).

### Sink
1. Clearer sink beat: tilt → half-submerge → remove, ~1.5–2.5s; simple bubbles/foam particles OK.
2. Keep `sfx_sink` Water Splash 02 + brief BGM duck.
3. Camera: lightly follow enemy wreck then return to player; on player fail, follow sink then fail UI (close = restart stays).
4. Loot small card appears **after** sink beat starts/settles — don’t stack VFX and popup same frame.

### Out of scope
Real debris models, per-shot damage numbers, new BGM packs, herbs/equipment.

## Success
Medium feedback feel as above; Release **v0.28.26**.
