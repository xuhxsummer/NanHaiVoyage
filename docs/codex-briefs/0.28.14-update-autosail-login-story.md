# NanHaiVoyage 0.28.14 — Update dialog, auto-sail ignore pirates, login helpers, main-quest story

## Model
`gpt-6-astra` **high**, `/fast` **off**, never luna. Visible Codex TUI in `~/shipgame`.
First reply: quota OK or usage-limit/reset time.

## User asks (4 items)

### 1. Polish update/download popup (太不明显了)
- Screenshot of current plain modal: `assets/ui-refs/19-update-download-plain.jpg`
- Code: `LoginScreen.java` `showUpdatePrompt` / `showDownloadPanel` (uses `panelGold` but still reads as a thin dark box on device).
- Make both **发现新版本** and **正在下载… N%** panels match Tang/大唐海贸 login chrome: thicker gold frame, darker opaque fill, larger title, thicker progress bar (cinnabar/gold fill), readable cancel. Dim backdrop optional. Keep full-APK update flow (no hot-update).

### 2. Auto-sail: ignore pirates (currently stops)
- Bug: `GameState.spawnPirate()` calls `stopAutoSail()` (~L1518). Also `startAutoSailToPort/Isle` refuse when `pirateAlive` (~L1401/1413). Flee clear also `stopAutoSail()` (~L1528).
- Desired: **while auto-sailing, keep course past pirates** — do not cancel auto on spawn/flee. Allow starting auto-sail even if a pirate is alive. Manual players can still fight (lock/fire unchanged). Soft toast OK (“遭遇海盗，自动航行继续…”); do not force combat. If auto ends at dock while pirate alive, existing dock pause is fine.

### 3. Login 公告 / 客服 / 设置 (stubs today)
- `LoginScreen` helpers currently `msg.setText(text + "暂未开放。")`.
- Implement usable Scene2D overlays (same login skin / gold panels), dismissible:
  - **公告**: version + short changelog bullets for current build (hardcoded Chinese OK; pull from release notes style).
  - **客服**: contact / help copy (email or “本机游戏，反馈发开发者” placeholder — no real backend).
  - **设置**: local prefs — at least music/SFX mute or volume toggles if audio exists; otherwise quality/vibration stubs + “本机存档” note. Persist with `Gdx.app.getPreferences`.
- Restyle the three top-right buttons to match login chrome (not bare outlined boxes).

### 4. Main-quest story — **YOU write the copy**
- Setting: **武则天 / 武周** era Great Tang sea trade; player is a merchant captain searching 奇珍异兽, exploring islands, 《山海经》 beasts, herbs — exploration + trade tone, not pure war.
- Hooks: `VoyageScreen` `QuestDef` / `QUESTS` (~L1386+). Today titles/descs are dry tutorials (“首次登岛”, “买点货”…).
- Deliver:
  1. Keep existing progress types / unlock chain / rewards working (do not break claim logic).
  2. Rewrite titles + descriptions with short narrative flavor (2–4 sentences max per quest), Wu Zhou flavor.
  3. Add a **story blurb** field (or reuse/extend desc) shown on quest detail — optional short chapter lines on accept/claim toast if easy.
  4. Optionally prepend 1–2 new prologue quests **only if** unlock chain stays coherent; prefer enriching existing 0–18 first.
  5. Put authored Chinese story text in code (or `android/assets/text/main-quest-story.md` loaded once) so Summer can iterate.
- Tone: literary but readable on phone; no walls of text; place names may use existing Catalog ports/islands.

## Out of scope
Close-up naval combat, cloud save, Meshy, new art packs, real customer-service backend.

## Package
```bash
./scripts/release.sh 0.28.14 "更新弹窗换皮；自动航行无视海盗；登录公告客服设置；武周主线剧情"
```
APK gitignored under `Builds/`. Report GitHub Release URL when done.

## Success
- Update panels clearly Tang-styled vs `19-update-download-plain.jpg`.
- Auto-sail does not stop on pirate spawn; can start with pirate alive.
- 公告/客服/设置 open real panels.
- Main quests read as Wu Zhou exploration story; compile + Release OK.
