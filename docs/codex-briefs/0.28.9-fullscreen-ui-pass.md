# NanHaiVoyage — Full-screen UI pass (one package)

## User mandate (2026-09-08 evening)
Summer finds popup text too small; enlarging text in small dialogs looks ugly. **Root fix: make overlays full-screen** (left nav + right content where it fits), then she will tune after seeing the whole build.

### Keep as non-fullscreen / current port-island UX
- **Port dock / port interaction** (`Overlay.PORT`, `PortDockPanel`, in-port **MARKET** buy/sell reached from port)
- **Island interaction** (`Overlay.ISLAND`)
These stay as today’s interaction panels — do **not** force them into the new full-screen shell.

### Convert to full-screen (stage-filling or ~1296×832+ like `MyShipPanel` / `ShopShipsPanel`)
Replace narrow ~520–560 centered popups and any remaining mid-size “dialog” feel with **full-screen overlays**:

1. **船长 / 头像** (`Overlay.AVATAR`) — replace `CaptainMenuPanel` popup with full-screen page:
   - Left nav: 个人信息｜存档｜调试(满级账号)｜账号(退出登录)
   - Right: content for selection
   - **个人信息**: editable **昵称** + pick among **4 avatars** (`textures/avatars/avatar_01..04.png`, placeholders OK); persist `nickname` + `avatarIndex` in save; HUD top-left circle updates live
2. **任务列表** (`Overlay.QUESTS`) — full-screen; left list / right detail preferred; larger readable type
3. **货舱** (`Overlay.CARGO`) — full-screen if not already
4. **情报** (`Overlay.INTEL`) — full-screen shell (may already be wide; make truly stage-filling + readable)
5. **图鉴** (`Overlay.CODEX`) — full-screen shell
6. **商城买船** (`Overlay.SHOP` / `ShopShipsPanel`) — ensure full-screen
7. **我的船** (`Overlay.MINE` / `MyShipPanel`) — ensure full-screen
8. **属性详情** (`Overlay.STAT`) — full-screen or large panel, not tiny popup
9. **新手说明** (`Overlay.HOWTO`) — full-screen readable
10. **失败/沉船等** (`Overlay.FAIL`) — full-screen readable
11. **钓鱼** (`Overlay.FISH`) — full-screen if currently a small dialog
12. Any other voyage HUD-opened overlay that is still a small centered dialog — same treatment (except PORT/ISLAND/MARKET-from-port)

### Layout pattern
Prefer:
```
┌ title + 关闭 ──────────────────────────┐
│ LEFT nav/list  │ RIGHT detail/content   │
└────────────────┴────────────────────────┘
```
World still **pauses** while these are open (same as today). Match Tang/paper `QuestUi` chrome. Bump body text to comfortable sizes now that space exists (avoid huge empty margins with tiny type).

### Also in this same release if 0.28.8 not shipped yet
Wire `textures/ports/` + `textures/islands/` (+ beasts/herbs assets if cheap) per `docs/codex-briefs/0.28.8-port-island-icons.md`. If 0.28.8 already released, skip duplicate work.

### Process
- `gpt-6-astra` max, `/fast` off, visible Codex terminal
- First line: quota OK or usage-limit/reset
- One package when the full-screen pass is playable:  
  `./scripts/release.sh 0.28.9 "全屏UI：任务船长等；港岛交互保持"`  
  (bump patch if 0.28.9 taken)
- Fix smokes that assume old popup sizes/names
- Report Release URL; list which overlays changed

## Success
Opening 任务 / 船长 / 货舱 / 情报 / 图鉴 / 商城 / 我的 / 属性 / 玩法说明 feels like full-screen game pages with readable text; only 港/岛(+港内市场) keep the old interaction style.


## Superseded
Merged into `docs/codex-briefs/0.28.9-voyage-feel-and-fullscreen.md` (one package with steer/look/scale).
