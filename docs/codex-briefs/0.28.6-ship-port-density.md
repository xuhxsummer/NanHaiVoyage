# NanHaiVoyage — denser ship/port art for 0.28.6

## Model
- Prefer Astra / strongest available Codex model.
- Use high reasoning if available. If /fast is currently on, turn it off for this art pass only (user previously wanted high for ship/port density). If quota is tight after refresh, /fast is acceptable fallback — say which you used.
- Visible terminal only (already running). Do not use headless `codex exec`.

## Repo
`/home/box/shipgame` (libGDX Java Android). Base: v0.28.5 sea already shipped.

## Goal
Raise visual density of in-engine 3D ships + Tang ports toward:
- `assets/ui-refs/12-voyage-3d-chase-ref.png`
- `assets/ui-refs/14-port-style-ref-2d.jpg`

Requirements:
- Detailed player junk: multi-sail panels, thin-box rigging, stern castle, hull planks, optional sail emblem; keep ship variants from GameState.ship
- Denser Tang ports: roof variety, street depth, lanterns, pier junks, stronger mountain backdrops; cohesive wood/roof/water materials with 0.28.5 sea
- Preserve collision, chase cam, HUD, local saves, update UI, max-account, Android FPS (LOD distant ports OK)
- No Meshy / no FreeBuff for this task
- Do NOT package until the art work is actually done; then package exactly:
  `./scripts/release.sh 0.28.6 "船港美术加密度"`

## Success
Release URL, OR exact usage-limit / quota message with reset time if blocked.

## First reply
Immediately acknowledge whether you can work (quota OK) or paste the exact quota/reset message. Then continue the task if OK.
