# NanHaiVoyage 0.28.11 — pack clarity, world×2 again, shop merge, daily login, pirate no-overlap

## Model
`gpt-6-astra` **high**, `/fast` **off**, never luna. Visible Codex terminal.
First reply: quota OK or exact usage-limit/reset.

## In scope (user confirmed 2026-09-09)

### 1) Packaged art max edge **1024** (was 512)
In `build.gradle` `prepareWaterAssets` / pack-time downsample: raise cap from 512 → **1024** for ports/islands (and beasts/herbs if same path). Keep masters in `android/assets`; only packaged copies are capped. APK stays **gitignored**; larger Release asset OK.

### 2) 「活动」= fullscreen **daily login reward**
Replace toast `活动尚未开放` with a fullscreen page (same family as other fullscreen UI).
- Simple daily claim: once per game-day (use existing game clock / day counter) grant a fixed silver amount (pick a sensible default e.g. **200** 银两 unless a constant already exists — document in release notes).
- Show claimed vs available state; persist per save.
- Not a complex event system.

### 3) World scale **×2 again**
Current `Catalog.WORLD_W/H` is 9600×7200 after 0.28.10. Double again (~**19200×14400**), scale all port/island coords and dependent land/spawn/ranges consistently, migrate old saves like 0.28.10 did. Retune dock/camera/minimap/far plane as needed.

### 4) Merge 船坞 + 商行 → one **商城** with categories
HUD rail: remove duplicate 船坞/商行 entries; single **商城** entry.
Inside fullscreen shop: **tabs/categories** (at least **船只**; add empty or stub categories only if needed for UI — prefer ships + any existing buyables grouped clearly). Wire existing buy-ship / my-ship flows without breaking smokes.

### 5) Pirate ships must not overlap / sink into player
User: pirates keep closing until models overlap — unrealistic; should **collide / stop / slide**, not occupy the same space. Fix chase/combat movement so pirate maintains a minimum separation (hull envelopes / collision already used for land — extend to pirate↔player). No tunneling when closing at speed.

## Explicitly out of scope
- Close-up naval combat camera pass
- 草药使用、装备栏、船队托管、上岛自走、云存档
- True hot-update / differential patch (keep full APK Release flow)

## Package
```bash
./scripts/release.sh 0.28.11 "贴图1024；每日登录；世界再×2；商城合并；海盗防重叠"
```
APK not committed. Report Release URL.
