# NanHaiVoyage 0.28.26 — Guide modal / dialogue busts / activity merge / chase AI / HUD (FreeBuff GLM)

## Agent
**FreeBuff** visible terminal `~/shipgame`, **GLM 5.3 Flash**.
Base: `main` (= v0.28.25 merged package).

## Package (after all 7 done)
```bash
./scripts/release.sh 0.28.26 "引导大弹窗暂停；对话立绘左右；活动含福利；海盗贴脸追打；取消锁定叠自动航行；顶部轮播提示；去掉我的船只港口"
```
One Release. Report URL.

User screenshots: tip toast too small; dialogue busts inside text both sides; 福利+活动 separate; pirate kites at range edge; cancel-lock far from auto-sail; status toast at bottom; 我的船只/港口/情报 text links.

---

## 1) Contextual tips = port-style modal + world pause
First-login tips (stick/dock/pirate_lock/…) must NOT be tiny toast bars.
- Use a **centered modal** similar to port/island popup weight (panel + 知道了).
- **Pause world time** while tip is open (`overlay` / worldPaused path like other menus).
- Dismiss on 知道了 or completing action; same once-per-tip-per-login rules.

## 2) Dialogue paper busts layout
- Bust sits at **top-left of the bottom dialogue frame** (outside/above the text block), not embedded inside text wrapping.
- **Only active speaker’s side shows a bust**: left speaker → left bust only; right/「我」→ right bust only. Never both at once.
- Keep paper-cutout assets; fix `QuestDialogue` layout.

## 3) Merge 福利 into 活动
- Remove top-right rail icon **福利**.
- Keep **活动**; put redeem/welfare (兑换码等) inside the Activities page (tabs or sections).
- Remap old 福利 shortcut to open 活动 (welfare section if easy).

## 4) Locked pirate/warship chase — too much edge-kiting
Current 0.28.25 preferred band ~60–90% PIRATE_RANGE feels like forever circling the ring; turning away seems to end fight (bug if lock clears / AI gives up).

Locked rules (remind + tighten):
- **Locked + HP > 50%**: actively **close in and chase** the player (flank OK but allow closer band, e.g. ~35–70% range); keep firing; **warships keep probabilistic ram** when opportunity.
- **HP ≤ 50%**: flee while return-fire (only then “边打边跑”).
- Unlock / sail away: don’t instantly freeze — short continue then drop; fix unintended combat stop when player briefly turns opposite while still locked and in horizon.
- Merchants unchanged (flee+fire, never chase).

## 5) 取消锁定 above 自动航行
Bottom-left: stack **取消锁定 directly above 自动航行** (same column, tight vertical), beside helm. Not far mid-screen.

## 6) Status toasts to top + marquee
Move bars like「已锁定海盗…」「撞击！…」from bottom to **top** (under/near resource row, not covering rail badly).
If text wider than bar: **horizontal marquee / scroll** loop.

## 7) Remove 我的船只 & 港口; restyle 情报
- Delete top-left text links **我的船只** and **港口** (ship/port still reachable via existing avatar/port-near flows).
- Restyle **情报** like top-right circular rail badges (icon + label).

## Out of scope
Hermes, new story chapters, herb/equipment.

## Success
All 7; Release **v0.28.26**.
