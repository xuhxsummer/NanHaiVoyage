# NanHaiVoyage 0.28.13 — Login motion + form contrast (user: 太不明显了)

## Model
`gpt-6-astra` **high**, `/fast` **off**, never luna. Visible Codex.
First reply: quota OK or usage-limit/reset.

## Feedback
User screenshot: `assets/ui-refs/18-login-too-subtle.jpg` after v0.28.12.
Complaint: **太不明显了** — dynamic sea/sails are too subtle; login chrome also sinks into the busy harbor art.

## Goals
1. **Much more obvious motion** on login (still lightweight, mid Android OK):
   - Sea: stronger ripple amplitude / faster readable flow (not a tiny shimmer).
   - Sails & flags: larger sway angles / clearer wind pulse so motion is obvious in a few seconds of watching.
   - Keep layered approach from 0.28.12; tune constants in LoginScreen / login shaders / layer rates in `layers.json` as needed.
2. **Login form readability**: darker/more opaque panel behind fields, thicker gold borders, larger labels, higher-contrast 登录/注册 buttons so controls pop against the painting (match spirit of `17-login-dynamic-ref.png` without killing the art).

## Out of scope
Voyage 3D sea, new account backend, full CS/announcement systems.

## Package
```bash
./scripts/release.sh 0.28.13 "登录动效加强；登录框更醒目"
```
APK gitignored. Report Release URL.
