# NanHaiVoyage 0.28.27 — HUD / map / dialogue / island buttons (Codex)

## Agent
**Codex** visible terminal `~/shipgame` (gpt-6-astra, reasoning **high**, `/fast` off). Never `codex exec`.
Base: current v0.28.27 work; retain items 1–11 and finish items 12–13 and refresh the same release. Preserve existing test WIP.

## Package (ONLY after all 13 done)
```bash
./scripts/release.sh 0.28.27 "HUD map dialogue island"
```
Report Release URL.

---

## 1) Move 情报 into minimap
- Remove 情报 from the top-right circular rail row.
- Place 情报 control at the **minimap bottom-left** (replace the small icon currently there — e.g. the compass/「世界」corner control).
- Tapping still opens Intel panel.

## 2) Shrink「南海地图」title plate
- World-map (and any minimap-adjacent) title box labeled **南海地图** is too wide — excess empty space on the right of the text.
- Tighten width to fit title + subtitle (点港口/岛屿自动驶向) with modest padding; no long empty strip.

## 3) Remove 银两 from main HUD
- Remove silver (银两) from top-left resource row on voyage HUD.
- Keep silver visible inside shop/port/cargo popups as today.

## 4) 自动航行 — text only
- Bottom-left Auto-sail button: remove leading icon (helm/star ico); keep label「自动航行」only.

## 5) Dialogue width + remove「点击继续」
- Bottom story dialogue panel too wide — narrow it (centered, readable column, not nearly full screen width).
- Remove visible「点击继续…」/「点击继续 · n/m」hint text (tap-to-advance can remain without that caption).

## 6) Warship-discovery tip — center + quest-card chrome
- Tips like「远处发现战船…」are too left; **center horizontally** in the mid/top tip band.
- Restyle UI to match right-side main-quest card look (e.g. 主线·武周启帆 — parchment/header chrome), not a plain dark toast bar.

## 7) Island near-ship actions
When near island (e.g. 川山):
- Do **not** show island name「川山」in the floating panel (name already on map/water).
- Remove subtitle「点击登岛·搜采奇珍」.
- Replace name slot with button **探索** (opens island explore / same as former dock/search action).
- Stack **探索** above **起锚** vertically (same column, beside ship).

## 8) Remove minimap weather plate
- Remove the entire「今日：晴」plate under the minimap.
- Do not leave its frame or icon behind.

## 9) Weather in bottom-left clock with Chinese ordinal days
- Bottom-left clock uses `第一天 16:21 白天 晴`.
- Use 第一/第二/第十/第十一/第二十一/…天; never `第1日` or Arabic day digits.
- Keep live HH:MM and 白天/夜晚, with weather appended after the day/night word.

## 10) Remove debt and cargo from top-left status
- Remove debt「欠N」and cargo「舱 used/cap」from the top-left HUD strip.
- Preserve the underlying debt/cargo data and detailed popup displays.

## 11) Wind and speed in bottom-right HUD
- Move wind (逆风/顺风/侧风) and speed「速N」from the top-left status to bottom-right.
- Keep values live, readable against the sea, within viewport, and clear of quest cards.

## 12) Quest card markers only on title bars
- On both 主线 and 下一程 cards, keep exactly two small outline △ markers: one left and one right of the title, inside its header bar.
- Remove the four white corner triangles from the card body/corners.
- Header backgrounds must not add another set of corner triangles.

## 13) Quest header flush alignment
- Align each header bar with its card's left edge, using the same width as the card.
- No thin left gutter, asymmetric outer padding, or mismatched background inset.
- Keep readable title spacing and existing card tap actions.

## Out of scope
New systems, Hermes, story rewrite.

## Success
All 13 items verified before packaging. Refresh Release **v0.28.27** with the updated APK and matching tag; do not create a new version.
