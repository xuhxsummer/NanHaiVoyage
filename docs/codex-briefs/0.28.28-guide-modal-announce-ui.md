# NanHaiVoyage 0.28.28 — Unify guide/tip modals with login announcement chrome (FreeBuff)

## Agent
**FreeBuff** (GLM) visible terminal in `~/shipgame`. Keep Codex window open if present; do not close it.
Base: `main` (= latest v0.28.27).

## Package (only after all items done)
```bash
./scripts/release.sh 0.28.28 "指引弹窗统一登录公告描金UI"
```
One GitHub Release. Report URL to Grok Bot.

## Visual target (must match login 公告)
Refs in this folder:
- `refs-0.28.28-login-announce.jpg` — **TARGET** style (login 公告 modal)
- `refs-0.28.28-voyage-tip-current.jpg` — **CURRENT** voyage「航海指引」modal to replace

Login announcement chrome (see `LoginScreen.java`):
- Panel fill **`#081522`** (opaque navy), **gold double rim** via `frame("modalFrame", "081522")` — outer `#D8AE60`, highlight `#FFF0B5`, shade `#70502C`
- Dimmer behind: dark navy ~`rgba(0.01,0.02,0.04,0.72)`
- Title: gold `#FFF0BF`, larger centered
- Body copy: warm gold/cream `#F4E6CB` (login `modalCopy`), left or center as fits; **not** parchment/paper cream from `QuestUi.PAPER` on teal panel
- Bottom button: gold-rim navy (`modalBlue` / `modalHover` style), gold text — login uses「关闭」; tip keeps「知道了」
- Sharp corners, thick gold rim readable on phones (same NinePatch recipe as login)

## Scope

### 1) Contextual tip modal (`VoyageScreen` tipBubble / tipPanel)
Built around ~L375–390:
- Today: `voyageHud.ui.panel` + `QuestUi.PAPER` labels + blue `知道了` — looks like teal parchment with corner triangles (current screenshot).
- Change to **login-announcement visual**: same fill/rim/title/body/button language as `LoginScreen.openModal` + `modalCopy` + `modalButton("关闭"…)`.
- Prefer extracting a tiny shared drawable helper (e.g. gold-rim navy NinePatch identical to login `frame`) under `ui/` so voyage + login stay in sync — or duplicate the pixmap recipe once in voyage chrome if sharing is messy. **Do not** restyle the whole HUD / quest parchment.
- Keep behavior: world pause while open, once-per-tip-per-login, dismiss on「知道了」, copy from `ContextualTips` unchanged.
- Size: still comfortable centered modal (~720×340 or slightly taller if gold chrome needs pad); title「航海指引」stay.

### 2) Other **指引 / 文字说明** popups that use the same old tip chrome
Apply the **same announce chrome** to any other short instructional modal that still uses `voyageHud.ui.panel` + PAPER for “how to play” style tips (e.g. first-run howto if it is still the tip-style panel, or any leftover toast-as-panel guide).  
**Out of scope:** quest dialogue paper busts, port/island full-page `pageFrame` menus, cargo/mall/settings pages, sink/loot combat panels — unless they are literally the same tipPanel builder.

### 3) Version string hygiene
Bump any hard-coded login announcement version text that still says `0.28.16` to **0.28.28** when touching `LoginScreen.showHelper("公告")` (optional one-liner if you already edit that file).

## Out of scope
Hermes, new story, ocean art, combat AI, HUD layout changes beyond tip chrome.

## Success
- Side-by-side: voyage tip looks like login 公告 (navy + gold rim + gold type + gold-rim button).
- Tips still pause world; dismiss works; Release **v0.28.28** URL reported.
