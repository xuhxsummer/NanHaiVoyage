# NanHaiVoyage — Wire port + island map icons (0.28.8)

## Context
v0.28.7 already shipped (realistic sea + 9 ship icons). New Gemini art is on disk, untracked:
- `android/assets/textures/ports/port_*.png` — 21/21 matching `Catalog.PORTS`
- `android/assets/textures/islands/island_*.png` — 13/13 matching `Catalog.ISLANDS` (see `islands/MANIFEST.md` for source-name mapping)
- Still untracked from earlier: `textures/beasts/`, `textures/herbs/` — include in the same release if cheap to wire or at least commit into APK assets

## Goal
1. Wire port icons into map / port markers / port UI where placeholders exist.
2. Wire island icons similarly.
3. Prefer Catalog name → `port_<name>.png` / `island_<name>.png` paths.
4. Do not redesign systems; smallest robust asset hookup.
5. Package: `./scripts/release.sh 0.28.8 "港口岛屿图标入库并接到海图"`
6. Model: gpt-6-astra max, `/fast` off. Visible terminal. First line: quota OK or usage-limit/reset.

## Out of scope
Combat, water shaders, saves. Ports/islands 3D meshes already exist — this pass is **2D icon art**, not rebuilding 3D harbors unless icons are unused without a tiny hook.
