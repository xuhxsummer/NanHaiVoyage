# NanHaiVoyage 0.28.21 — Login center + controls polish + drift + anchor/dock tip (FreeBuff / GLM)

## Agent
**FreeBuff** visible terminal `~/shipgame`, **GLM 5.3 Flash**.
Base: current `main` (= v0.28.20).

## Package
```bash
./scripts/release.sh 0.28.21 "登录框居中；收掉右侧加减速；加强未抛锚漂流；抛锚贴船；停靠提示置顶；15秒自动存档"
```
APK gitignored. Report Release URL.

## Refs
- `assets/ui-refs/21-login-form-left.jpg` — login form feels left-biased
- `assets/ui-refs/22-anchor-dock-hint.jpg` — dock tip mid-screen; anchor not by ship; accel/decel still prominent bottom-right

---

## A) Login form truly centered
`LoginScreen` uses hard `place(formPanel, 504, 352, 912, 304)` on a fixed design size — looks left on some aspect ratios.
Re-center the **login/register form panel** (and related fields) horizontally (and keep vertical sensible). Also ensure success/update modals stay centered (`centerModal` after pack/layout). Gate progress bar can stay bottom-centered.

## B) Right accel/decel — hide or demote
User: 「方向盘和右边加速减速按钮没有什么变化」.
0.28.17 already made left stick thrust+steer; right buttons still visible as fine control.
**This build: hide 加速/减速 buttons by default** (or remove from HUD). Left stick alone must drive. Do not add sprint. Optional: keep keyboard W/S if already there.

## C) Stronger drift when near port/island without anchor
User: not anchoring near land barely drifts away.
Increase coast/current drift when `canAnchor()` / in land range and **not** `anchored`, so ship clearly slides out of interact range if player idles. Keep anchored = fully stopped. Tune so it's obvious in ~few seconds without feeling broken in open sea (open-sea coast can stay milder).

## D) Anchor button beside the ship
Move 抛锚/起锚 control from top/utility rail to **near the player ship on screen** (world-to-screen follow, or fixed offset above/beside ship sprite in HUD space). Still only visible when in port/island range (or already anchored). Easy to tap; don't cover the ship core.

## E) Dock hint to top
「广州港 / 点击停靠 · 港口经营」style place banner currently mid/upper-mid — **move to top of screen** (below status bar / safe top), full width or centered top band. Same for near-island wording.

## F) Autosave every 15s
Overwrite local account save periodically while logged in (voyage / menus). **Does not grow APK** — local prefs/files only. No new manual-save UX required; remove or keep any existing save button inert/hidden if present. Debounce: skip if no dirty state when cheap to detect.

## Out of scope
Differential update, sea-map redo, audio re-pick.

## Success
Form centered; no right accel/decel; clear near-land drift; anchor by ship; dock tip at top; 15s autosave; Release **v0.28.21**.
