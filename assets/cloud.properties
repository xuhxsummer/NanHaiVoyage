# DISABLED 0.27.7 — client uses device-local accounts + saves (saves/<user>.json).
# Cloud backend paused until a rented server is ready (server/ kept in repo).
# When re-enabling: put the single API base URL on one line below, e.g.
# https://your-server.example.com
